# 课设交接文档

**项目**：基于 RAG 与知识图谱融合的高校智能问答系统  
**本次交付**：插件化 + 远程通讯（分布式）**基座**；`vector_db` 组模块已落地（见第 11 节）  
**交付目录**：`campus-qa-system/`  
**日期**：2026-09-21（基座）/ 2026-09-22（vector_db 模块）  

---

## 1. 目标与范围

| 本次已做 | 本次不做（后续按组补） |
|----------|------------------------|
| 插件契约 `Plugin` / 注册表 / 动态加载 | LLM 生成、Neo4j 图谱 |
| RPC 协议 + 节点 HTTP 服务 + 远程代理 | 完整业务流水线 gateway |
| 拓扑配置 `config/topology.yaml` | 生产鉴权、TLS、服务发现 |
| echo 示例插件 + 自检脚本 | 界面 |
| 四组业务占位与边界说明 | 各组具体算法实现 |
| **`vector_db`：`embedder` + `vector_store` 已落地**，含 40 题评测集与阈值标定（第 11 节） | **拒答/澄清判定**（属 qa 编排层） |

设计原则：**一组业务 = 一个可独立部署的节点**；调用方通过同一 `Plugin` 接口访问本地或远程插件。

---

## 2. 业务四组边界

| 组 | 节点建议名 | 端口占位 | 应提供插件（建议名） | 只负责 | 禁止 |
|----|------------|----------|----------------------|--------|------|
| 文件管理 | `file_mgmt_node` | 8101 | `file_store`, `doc_parser` | 原文存取、解析拆解、chunk 元数据 | 向量/图谱/生成 |
| 向量数据库 | `vector_db_node` | 8102 | `embedder`, `vector_store` | 向量化、入库、相似检索 | 解析文档、生成答案 |
| 知识图谱 | `kg_node` | 8103 | `kg_builder`, `kg_query` | 实体关系构建与查询 | 答案生成 |
| 知识问答 | `qa_node` | 8104 | `retriever`, `generator` | 编排远程插件、生成答案与溯源 | 直接读磁盘/库细节 |

Gateway（可后续放在 `qa_node` 或独立节点）负责按流水线调用其它组。

---

## 3. 目录结构

```
campus-qa-system/
├── README.md
├── docs/
│   └── 交接文档.md          # 本文
├── base/                     # 基座（不要在此写业务）
│   ├── sdk/
│   │   ├── base.py           # Plugin, PluginMeta
│   │   ├── registry.py       # PluginRegistry
│   │   └── loader.py         # entry 动态加载
│   ├── rpc/
│   │   ├── protocol.py       # JSON-RPC 信封与错误码
│   │   ├── client.py         # RpcClient, RemotePluginProxy
│   │   └── server.py         # FastAPI 节点服务
│   ├── config_loader.py
│   └── node.py               # python -m base.node --node <名>
├── plugins/                  # 业务插件（四组）
│   ├── file_mgmt/
│   ├── vector_db/            # 已落地：embedder, vector_store（+ README.md）
│   ├── kg/
│   └── qa/                   # retriever 已改为向量库薄封装（v0.2.0）
├── evals/vector_db/          # 向量库语料 + 评测集 + 评测/标定脚本
├── examples/
│   └── echo_plugin.py        # 基座示例插件
├── config/
│   └── topology.yaml         # 节点拓扑 + 插件 entry
├── data/                     # 运行数据（按节点分目录）
└── smoke_base.py             # 基座自检
```

---

## 4. 核心约定（必读）

### 4.1 插件接口

实现 `base.sdk.Plugin`：

```python
from base.sdk.base import Plugin, PluginMeta

class MyPlugin(Plugin):
    meta = PluginMeta(name="doc_parser", group="file_mgmt", version="0.1.0")

    def initialize(self, config=None): ...
    def methods(self):            # RPC 可调方法列表
        return ["split"]
    def call(self, method, params):  # 分发
        if method == "split":
            return self.split(**params)
        raise KeyError(method)
```

- `meta.name` 必须与拓扑配置里 `plugins` 的键一致（RPC 前缀）。
- `call` 的返回值必须可 JSON 序列化。
- `initialize` 中读 `config`，不要在模块 import 时做重逻辑。

### 4.2 RPC 协议

- 端点：`POST {base_url}/rpc`
- Body：`{"id":"...", "method":"{plugin}.{method}", "params":{...}}`
- 成功：`{"id":"...", "result": ...}`
- 失败：`{"id":"...", "error":{"code":int,"message":"..."}}`

错误码：

| code | 含义 |
|------|------|
| -32600 | 非法 method（无插件前缀等） |
| -32601 | 插件不存在 |
| -32602 | 方法不存在 / 参数相关 KeyError |
| -32000 | 插件内部异常 |

辅助端点：`GET /health`、`GET /plugins`。

### 4.3 远程调用

```python
from base.rpc.client import RemotePluginProxy
from base.sdk.base import PluginMeta

parser = RemotePluginProxy("doc_parser", "http://127.0.0.1:8101",
                           meta=PluginMeta(name="doc_parser"))
parser.initialize()   # 会调 doc_parser.info
result = parser.call("split", {"text": "...", "filename": "a.md"})
```

编排层只依赖 `Plugin` 接口，**不要**在业务代码里写死 HTTP。

### 4.4 拓扑配置

`config/topology.yaml`：

```yaml
nodes:
  file_mgmt_node:
    group: file_mgmt
    host: 127.0.0.1
    port: 8101
    data_dir: data/file_mgmt
    plugins:
      doc_parser:
        entry: plugins.file_mgmt.doc_parser:DocParserPlugin
        config:
          chunk_size: 200
```

- `entry` 格式：`package.module:ClassName`（相对 `campus-qa-system/`）
- `config` 原样传入 `Plugin.initialize`
- 数据路径建议放在对应 `data_dir` 下，避免节点互写

---

## 5. 如何运行

```bash
cd campus-qa-system
pip install fastapi uvicorn pyyaml

# 1) 基座本地自检
python smoke_base.py

# 2) 启动示例节点
python -m base.node --node echo_node
# 监听 http://127.0.0.1:8200

# 3) 另开终端验证 RPC
python smoke_base.py --rpc
# 或
python smoke_base.py --rpc http://127.0.0.1:8200
```

启动业务节点（插件配好后）：

```bash
python -m base.node --node file_mgmt_node
```

---

## 6. 如何新增一个业务插件

1. 在 `plugins/<组>/` 下新建模块，继承 `Plugin`。
2. 实现 `initialize` / `methods` / `call`。
3. 在 `config/topology.yaml` 对应节点的 `plugins` 里加：
   ```yaml
   my_plugin:
     entry: plugins.file_mgmt.my_module:MyPlugin
     config: { ... }
   ```
4. `python -m base.node --node <节点名>` 启动。
5. 用 `RemotePluginProxy` 或 `smoke_base.py` 模式做 RPC 自检。
6. 跨组依赖：在 `initialize(config)` 里用 `RemotePluginProxy` 连其它节点，**地址写在 config**，不要写死在代码里。

### 建议实现顺序

1. **file_mgmt**：`file_store` → `doc_parser`（拆解）  
2. **vector_db**：`embedder` + `vector_store`（可先内存实现）—— ✅ **已完成**，见第 11 节  
3. **kg**：`kg_builder` / `kg_query`（可先内存图）  
4. **qa**：`retriever` 调远程 → `generator`（可先 mock LLM）—— ⏳ `retriever` 已接 vector_db，待补 `generator`  

---

## 7. 与仓库内其它目录的关系

| 路径 | 说明 | 交接建议 |
|------|------|----------|
| `campus-qa-system/` | **课设正式基座**，后续开发以这里为准 | 继续扩展 |
| `min-proto/` | 早期文件管理拆解草稿（含本地分块实现参考） | 可参考 `doc_parser` 分块思路，不要当正式入口 |
| `0.0/knowledge-base-system/` | 单体插件化实验（LangChain/配置/注册表） | 可对照接口设计，依赖较重，不直接合并 |
| `rag-kg-qa/` `rag-kg-qa-d/` | 其它历史尝试 | 仅作参考 |

---

## 8. 验收清单（基座）

- [x] `python smoke_base.py` 本地通过（echo.ping / echo.echo）
- [x] `python -m base.node --node echo_node` 可启动
- [x] `python smoke_base.py --rpc` 远程通过
- [x] `GET /health` 返回已加载插件列表
- [ ] 配置改 `greeting` 后重启节点，RPC 结果变化（配置驱动，交接后可选自测）

**已知实现备注**：`base/rpc/server.py` 不要加 `from __future__ import annotations`，否则 FastAPI 无法解析 `Request` 注解，RPC 会 422。

---

## 9. 待办与风险

| 项 | 说明 | 优先级 |
|----|------|--------|
| 文件管理插件落地 | 存储 + 拆解，RPC 化 | 高 |
| gateway 编排 | 读流水线配置，串 file_mgmt → vector_db → qa | 高 |
| 配置中远程地址 | plugins config 增加 `remote_url` 约定 | 中 |
| 依赖与环境 | 固定 `requirements.txt`（目前仅 fastapi/uvicorn/pyyaml） | 中 |
| 向量/KG 具体选型 | Chroma / Neo4j 可在插件内选用，基座不绑定 | 中 |
| 鉴权与多机部署 | 原型阶段可省略；课设演示可用本机多进程 | 低 |

**风险**

- 插件 `call` 返回非 JSON 对象会导致 RPC 失败 → 统一转 dict/list。
- Windows 路径与中文目录：数据请配置到 `campus-qa-system/data/...`。
- 多节点同时写同一 `data` 目录会冲突 → 严格按 `data_dir` 隔离。

---

## 10. 联调口令（给下一任）

1. 先跑通 `smoke_base.py` 与 `echo_node`，再写业务。  
2. 新插件必须能被 `RemotePluginProxy` 调用，否则视为未接入基座。  
3. 业务边界见第 2 节表；越界代码在评审时打回。  
4. 拓扑改动只改 `config/topology.yaml`，避免改基座代码。  

---

## 11. 向量数据库模块（vector_db）交付速览

> 本节只是索引。**详细内容以 `docs/vector_db交接文档.md` 为准**，接这个模块的人先读那一份。

### 11.1 三份文档

| 文档 | 什么时候读 |
|------|-----------|
| `docs/vector_db交接文档.md` | **第一个读这份** —— 接口契约、qa 组怎么接拒答/澄清、复现命令、组外改动报备 |
| `docs/vector_db设计文档.md` | 想知道「为什么这么设计」、面试被问方案取舍时 |
| `docs/待组内确认事项.md` | 想确认哪些事已定、还差什么 |
| `plugins/vector_db/README.md` | 只想看插件方法签名 |
| `evals/vector_db/README.md` | 想跑评测或改语料 |

### 11.2 交付物

- `plugins/vector_db/embedder.py`、`vector_store.py`（+ `__init__.py`）
- `evals/vector_db/`：语料 `corpus/os_course.txt`、40 题评测集 `eval_set.jsonl`、评测/标定/校验脚本
- `smoke_vector_db.py`（本地，零依赖）、`smoke_rpc_vector_db.py`（走基座 RPC）
- `plugins/qa/retriever.py` 已改为**薄封装**（方案 C）：对外接口不变，内部转发 `vector_store`

### 11.3 实测指标（3 章 23 节 3084 字 / 40 题 / top_k=3）

| 项 | 值 |
|---|---|
| 库内 Hit@1（标题切分块） | **88.9%** |
| 库内 Hit@3（标题切分块） | **92.6%** |
| MRR（标题切分块） | **0.907** |
| 推荐拒答阈值 τ | **0.185** |
| 无关题拒答率 | 100%（8/8） |
| 诱导题拒答率 | 0%（字面向量硬上限，**需编排层补可支撑性检查**） |

### 11.4 qa 组要做的三件事

1. **拒答判定**：用 `vector_store.search()` 返回的 `score` 对 τ=0.185 过闸。vector_db **不做**拒答判定。
2. **澄清判定**：三态口径（作答 / 澄清 / 拒答）。判据参考实现见 `evals/vector_db/check_clarify_rule.py`，可整段复制。锚定依据是 `stats()["titles"]`。
3. **诱导题兜底**：分数拦不住诱导题，需在编排层加可支撑性检查（问句问「具体数值/实现/步骤」时，确认检索块里真有这类内容）。

### 11.5 组外改动报备（3 个文件 + 1 处未改）

| 文件 | 改动 | 说明 |
|------|------|------|
| `config/topology.yaml` | 新增 `vector_db_node`(8102) 与两个插件 entry；给 `rag_retriever` 加 `vector_store_url` | 未动其它节点。**副作用：`qa_node` 需在 `vector_db_node` 之后启动**，见 11.7 |
| `plugins/file_mgmt/doc_parser.py` | 修复块间重叠不足；新增 `title` 切分模式（**默认关闭**） | 默认行为与改造前逐字节等价 |
| `plugins/qa/retriever.py` | 改为薄封装（方案 C） | 对外接口不变，三级后端注入兜底 |
| `gateway/app.py` | **未改** | 已实测：不传后端会命中第 3 级兜底自动建本地 store，demo 不受影响 |

### 11.6 运行前置

```bash
cd campus-qa-system
pip install fastapi uvicorn pyyaml

python smoke_vector_db.py            # 零依赖，先跑这个
python smoke_rpc_vector_db.py        # 走基座 RPC（需上面的依赖）
python evals/vector_db/run_eval.py --split heading
python evals/vector_db/calibrate.py --split heading
python evals/vector_db/check_clarify_rule.py
```

注意：`evals/vector_db/` 下的脚本用 `--split`，不是 `--mode`。

### 11.7 ⚠️ 节点启动顺序（多进程部署）

`rag_retriever` 的 config 里配了 `vector_store_url: http://127.0.0.1:8102`，
`initialize` 时会去调 8102 —— **未启动 `vector_db_node` 时 `qa_node` 会直接启动失败**
（不是降级，是抛 `HTTPError` 起不来）。实测确认。

```bash
python -m base.node --node vector_db_node    # 8102 先起
python -m base.node --node qa_node           # 8104 后起
```

要恢复 `qa_node` 独立启动：注释掉 `topology.yaml` 里那一行 `vector_store_url`，
`rag_retriever` 会自动创建本地 `vector_store` 实例。

**gateway 单进程 demo 不受影响**：`build_demo_registry()` 未传该字段，走本地兜底，
`smoke_fusion.py` / `smoke_api.py` 已实测通过。

---

*本文档与 `campus-qa-system/` 代码同步交付；若改接口，请同步修改本文第 4 节。*
